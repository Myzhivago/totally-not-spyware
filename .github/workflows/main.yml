name: TNS Build for iPad mini 2 (iOS 10.3.3) & ARM Server

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Tools
        run: |
          brew install closure-compiler
          sudo ln -s /usr/local/bin/closure-compiler /usr/local/bin/closure-js

      # 1. 编译针对 iOS 10 的 Curl - 强制跳过 gethostbyname 检测
      - name: Build Curl
        run: |
          cd glue/dep/curl-*
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          export CC="xcrun -sdk iphoneos clang -arch arm64 -miphoneos-version-min=10.0 -isysroot $SDK_PATH"
          
          # 关键：通过环境变量强行通过 configure 的网络库检测
          ./configure --host=arm-apple-darwin \
                      --with-darwinssl \
                      --enable-static \
                      --disable-shared \
                      --disable-manual \
                      ac_cv_func_gethostbyname=yes \
                      ac_cv_func_getaddrinfo=yes \
                      CC="$CC"
          
          make -j$(sysctl -n hw.ncpu)

      # 2. 编译 iOS 10 的 Meridian 和 doubleH3lix
      - name: Build Libraries
        run: |
          cd glue/dep/doubleH3lix && make headless
          cd ../Meridian && make all

      # 3. 核心补丁：修复 iOS 10.3.3 的 __availability_version_check
      - name: Patch and Build Payload
        run: |
          cd glue
          mkdir -p obj/c obj/c++
          
          # 注入 C 链接符号定义
          printf 'extern "C" { int __availability_version_check(int a, char** b) { return 1; } int _availability_version_check(int a, char** b) { return 1; } }\n' > src/compat.cpp
          xcrun -sdk iphoneos clang++ -arch arm64 -c src/compat.cpp -o obj/c++/compat.o -miphoneos-version-min=10.0
          
          # 物理修改 Makefile，确保补丁链接且不因为未定义符号报错
          sed -i '' 's/$(CXX) $(LDFLAGS) -o $@/$(CXX) $(LDFLAGS) obj\/c++\/compat.o -Wl,-undefined,dynamic_lookup -o $@/g' Makefile
          
          export JIT=1
          make

      # 4. 生成静态资源 (这些资源将被放在 Linux/armv7 Nginx 上)
      - name: Final Package
        working-directory: ./root
        run: |
          make

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TNS-iOS10-Success
          path: root/dist/
